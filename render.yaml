services:
  - type: web
    name: knightbot-md
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: OWNER_NUMBER
        sync: false
      - key: SESSION_ID
        sync: false
      - key: PORT
        value: 3000
      - key: TZ
        value: UTC
